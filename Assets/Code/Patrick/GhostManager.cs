using UnityEngine;

public class GhostManager : MonoBehaviour
{
    [SerializeField] private ghostOrb _ghostOrb;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
